services:
  - type: web
    name: autopent-backend
    env: python
    region: oregon 
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn backend:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
